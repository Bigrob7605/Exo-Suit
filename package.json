{
  "name": "agent-exo-suit-v5",
  "version": "5.0.0",
  "description": "Enterprise AI agent development platform with MMH-RS compression technology",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "lighthouse": "lighthouse http://127.0.0.1:8000 --output html --output-path ./test-results/lighthouse-report.html",
    "test:accessibility": "node tests/accessibility-test.js",
    "test:all": "npm run test && npm run lighthouse && npm run test:accessibility",
    "test:setup": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "axe-core": "^4.8.0",
    "lighthouse": "^11.4.0",
    "puppeteer": "^21.6.0"
  },
  "keywords": [
    "ai",
    "agent",
    "development",
    "platform",
    "compression",
    "mmh-rs"
  ],
  "author": "Agent Exo-Suit Team",
  "license": "MIT",
  "dependencies": {
    "@axe-core/puppeteer": "^4.10.2"
  }
}
